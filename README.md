
# 📧 Email ID Validator (Python)

### 📘 Overview
A Python project that validates email addresses using **Regular Expressions (Regex)**.  
It checks if an entered email follows the correct structure like `name@domain.com`.

---

### 🚀 Features
- Validates single or multiple emails  
- Uses **Regex logic** for format checking  
- Prevents invalid patterns  
- Useful for input validation in real projects  

---

### 🧠 Example Output
